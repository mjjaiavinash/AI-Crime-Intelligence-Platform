{
	"command": "sh -c 'python3 -m uvicorn backend.main:app --host 0.0.0.0 --port ${X_ZOHO_CATALYST_LISTEN_PORT}'",
	"build_path": "./",
	"stack": "python_3_12",
	"env_variables": {
		"APP_ENV": "production",
		"HF_HOME": "/tmp/hf_cache",
		"TRANSFORMERS_CACHE": "/tmp/hf_cache"
	},
	"memory": 1024,
	"scripts": {
		"build": "pip install -r requirements.txt"
	}
}
