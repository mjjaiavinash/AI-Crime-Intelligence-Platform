version: 1

services:
  backend:
    type: AppSail
    stack: python3.9
    memory: 1024
    instances: 1
    port: 8000
    root_dir: .
    build_command: pip install -r requirements.txt
    run_command: bash startup.sh
    health_check:
      path: /health
      interval: 30
      timeout: 10
